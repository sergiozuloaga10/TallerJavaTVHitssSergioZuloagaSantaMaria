package test;

public class TestMain {
    public static void main(String[] args) {
        //Sistema de facturacion, utilizando las siguientes entidades
        // -> Cliente
        // -> Producto
        // -> Factura
        // Un servicio generico con las siguientes operaciones:
        // -> Guardar (agregar) elementos
        // -> Listar los elementos
        // -> Buscar por ID
        // -> Eliminar por ID
        // Una interfaz generica identificable, que obligue a cada entidad tener un metodo getId()
    }
}
